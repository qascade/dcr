// Orchestrator service takes Run Transformation request validates it and spins up the trusted env container along with an output store.
// It also then sends the collaboration event to the trusted env.
package orchestrator
